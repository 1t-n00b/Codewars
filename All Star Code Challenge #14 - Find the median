function median(array) {
//code here
  let sort_arr = array.sort((a,b)=>a-b);
  let length = sort_arr.length;

    return length % 2!==0 ? sort_arr[(length-1)/2] :
    (sort_arr[(length)/2] + sort_arr[(length)/2 -1])/2;

 }
