function sumOfDifferences(arr) {
  let arr2 = arr.sort((a, b) => b - a);
//  console.log(arr2)
  let sum=0;
  for (let i = 0; i<arr.length-1; i++){
    sum +=(arr2[i]-arr2[i+1]);
  }
//   console.log(sum);
  return sum;
}
